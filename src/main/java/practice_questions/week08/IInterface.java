package practice_questions.week08;

public interface IInterface {
    // With interface you can use it as much as you want
    //You can extend more than one interface in a class
    //in interface if you define a variables, in child classes you can not change the value.
    //final is const
    //in interface, when you define something it has default; public final static keywords
    //use implements keyword to inherit

}
